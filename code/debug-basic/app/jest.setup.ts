import dotenv from 'dotenv';

// Cargamos las variables de entorno
dotenv.config();

// Puedes ejecutar cualquier otra inicialización necesaria antes de los tests
// Por ejemplo, configurar un logger, mocks de APIs, etc.
